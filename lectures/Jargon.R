help(function name)
example(function name)
seq() and colon sign (:) are the same
seq(start, end, increment)
rep("", times = ) to repeat sth however many times
vectors: same type of numbers, strings, logical values, etc.
c() creates new vector by combing by combining a list of values
vectorname[numeric index] to retrieve that individual value in the vector